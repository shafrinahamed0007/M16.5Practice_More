/**
 * Use a for...of loop to concatenate all the elements of an array into single string
 */

var numbers = ["Tom", "Tim", "Tin", "Tik"];
var concat = "";

for(number of numbers){

    concat = concat + number;
}

console.log(concat);



